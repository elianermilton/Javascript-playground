/* =================== the atelier’s foundation =================== */

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  min-height: 100vh;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: #ffdcd1;            /* pale salmon, candlelight glow */
  color: #3a2a35;                  /* soft plum for contrast */
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2.5rem 1.5rem;
  text-align: center;
  transition:
    background-color 0.5s ease,
    color 0.5s ease,
    font-size 0.3s ease;
}

/* =================== intro text =================== */

#intro {
  font-size: 0.95rem;
  opacity: 0.9;
  margin-bottom: 0.25rem;
}

#subintro {
  font-size: 0.9rem;
  opacity: 0.8;
  margin-bottom: 1.75rem;
  font-style: italic;
}

/* =================== atelier title =================== */

#title {
  margin-bottom: 0.75rem;
  line-height: 1.3;
}

.title-main {
  font-size: 1.9rem;
  letter-spacing: 0.04em;
  text-transform: lowercase;
  font-weight: 600;
}

.title-whisper {
  display: block;
  font-size: 0.95rem;
  letter-spacing: 0.08em;
  text-transform: lowercase;
  opacity: 0.7;
  margin-bottom: 0.25rem;
}

#subtitle {
  font-size: 0.95rem;
  opacity: 0.85;
  margin-bottom: 1.75rem;
}

/* =================== mode buttons and captions =================== */

#mode-buttons {
  display: flex;
  gap: 2rem;
  justify-content: center;
  align-items: flex-start;
  flex-wrap: nowrap;
  margin-top: 1.5rem;
}

.mode-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.65rem;
}

#mode-buttons button {
  padding: 1rem 2.5rem;            /* big cozy buttons */
  border-radius: 50px;             /* pill shape */
  border: 2px solid #e2b7cf;       /* soft rose border */
  background: #ffe9f4;             /* blush base */
  color: #3a2a35;
  font-size: 1.2rem;
  font-weight: 600;
  letter-spacing: 0.04em;
  text-transform: lowercase;
  cursor: pointer;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
  transition:
    background-color 0.25s ease,
    transform 0.15s ease,
    box-shadow 0.25s ease;
}

/* optional tiny distinction between modes */
#soft-mode {
  font-weight: 600;
}

#focus-mode {
  font-weight: 700;
}

/* hover and active states */

#mode-buttons button:hover {
  transform: translateY(-2px);
  background: #ffd5ee;
  box-shadow: 0 12px 28px rgba(0, 0, 0, 0.10);
}

#mode-buttons button:active {
  transform: translateY(0);
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
}

/* subtitles under buttons */

.mode-caption {
  font-size: 0.85rem;
  opacity: 0.85;
  width: 190px;
  text-align: center;
  line-height: 1.4;
}

/* =================== accessibility =================== */

#mode-buttons button:focus-visible {
  outline: 2px solid #f3a6cf;
  outline-offset: 3px;
}

/* =================== atelier title fade-in =================== */

#atelier-header {
  text-align: center;
  margin-bottom: 2.5rem;
  opacity: 0;
  animation: headerFade 2.5s ease forwards;
}

@keyframes headerFade {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.welcome-line {
  font-size: 1rem;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  opacity: 0.7;
  margin-bottom: 0.25rem;
}

.atelier-name {
  font-family: "Brush Script MT", "Lucida Handwriting", cursive;
  font-size: 3rem;
  color: #3a2a35;
  letter-spacing: 0.03em;
  font-weight: 400;
  animation: nameFade 3s ease-in 0.8s forwards;
  opacity: 0;
}

@keyframes nameFade {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/*===================== animations ===================== */
#eliane-anim {
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 1.25rem 0 1rem;
}


#eliane-anim img {
  max-width: 360px;      
  height: auto;
  image-rendering: crisp-edges;
  animation: atelierSoftBob 3.4s ease-in-out infinite;
  transform-origin: 50% 78%; /* pivot around lower center of body */
}

@keyframes atelierSoftBob {
  0% {
    transform: translateY(0) rotate(0deg);
  }
  20% {
    transform: translateY(-1px) rotate(-0.4deg);
  }
  40% {
    transform: translateY(0) rotate(-0.2deg);
  }
  60% {
    transform: translateY(-1px) rotate(0.4deg);
  }
  80% {
    transform: translateY(0) rotate(0.2deg);
  }
  100% {
    transform: translateY(0) rotate(0deg);
  }
}
